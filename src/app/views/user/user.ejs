<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Usuarios</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb" crossorigin="anonymous">

  </head>
  <body>
    <nav class="navbar navbar-dark bg-dark">
      <a href="" class="navbar-brand mx-auto">Usuarios</a>
    </nav>

    <div class="row">
      <div class="col-md-6 mt-4 text-center">
        <table class="table table-striped table-bordered">
          <tr>
            <!-- <th>ID</th> -->
            <th>Login</th>
            <th>User</th>
            <th>Accion</th>
          </tr>
          <% for (var i = 0; i < users.length; i ++) { %>
            <tr>
              <!-- <td><%= users[i].id %></td> -->
              <td><%= users[i].login %></td>
              <td><%= users[i].name %></td>
              <td>
                <a href="/delete/<%= users[i].id %>" class="btn btn-danger">Delete</a>
                <a href="/update/<%= users[i].id %>" class="btn btn-info">Update</a>

              </td>
            </tr>
          <% } %>
        </table>
      </div>

      <!-- <div class="col-md-6 mt-4 card">
        <form class="card-body" action="/user" method="post">
          <h3 class="card-title">Add /usuarios</h3>
          <div class="form-group">
            <input name="login" type="text" class="form-control" placeholder="Login">
          </div>
          <div class="form-group">
            <input name="name" type="text" class="form-control" placeholder="Nombre">
          </div>
          <input type="submit" class="btn btn-primary">
        </form>
      </div>
      -->
    </div>
  </body>
</html>
